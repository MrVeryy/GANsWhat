# ========= 基本 Python 環境設定 =========
# 把目前專案根目錄加到 Python 的模組搜尋路徑，方便 import
PYTHONPATH=.

# ========= CUDA / GPU 設定 =========
# 限制程式可見的 GPU 編號（多張 GPU 用逗號分隔；留空則只用 CPU）
# 例：CUDA_VISIBLE_DEVICES=0      -> 只用第 0 張 GPU
#     CUDA_VISIBLE_DEVICES=1      -> 只用第 1 張 GPU
#     CUDA_VISIBLE_DEVICES=0,1    -> 用第 0 和第 1 張 GPU
#     CUDA_VISIBLE_DEVICES=       -> 不用 GPU，強制 CPU
CUDA_VISIBLE_DEVICES=0

# 指定要編譯的 CUDA 架構 (Compute Capability)，避免編譯所有卡，縮短編譯時間
# 例：RTX 3060 = 8.6, RTX 3090 = 8.6, RTX 4090 = 8.9
# 可以用空白分隔多個，例如: "7.5 8.6"
# for more info here: https://developer.nvidia.com/cuda-gpus
TORCH_CUDA_ARCH_LIST=8.6

# ========= 其他常用變數 =========
# 控制 log 等級，避免顯示太多 debug 訊息
LOGLEVEL=INFO

# 設定生成圖片的輸出路徑（可在程式裡用 os.getenv("OUTPUT_DIR") 讀取）
OUTPUT_DIR=outputs